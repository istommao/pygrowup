# 字典

```python
dst = {'Hello': 'world', 'age': 26}

# 修改dict的值
dst['age'] = 25

# 使用update方法
dst2 = {'name': 'codingcat', 'height': 175}
dst.update(dst2)

# 使用in判断key是否存在
'name' in dst

# 使用key获取元素
print(dst['age'])
```
